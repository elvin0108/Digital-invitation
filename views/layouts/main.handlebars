<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} | {{eventDetails.name}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.2.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <meta property="og:title" content="Join me at {{eventDetails.name}}!" />
    <meta property="og:description"
        content="I'm attending {{eventDetails.name}} on {{eventDetails.date}} at {{eventDetails.venue}}. Click to create your own invitation!" />
    <meta property="og:image"
        content="{{posterUrl}}" />
    <meta property="og:image:width" content="1280" />
    <meta property="og:image:height" content="853" />
    <meta property="og:url" content="{{pageUrl}}" />
    <meta property="og:image:type" content="image/jpeg">
    <meta property="og:type" content="website" />
</head>

<body>
    {{> header}}

    <main class="container py-4">
        {{{body}}}
    </main>

    {{> footer}}

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.2.3/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>

</html>